# Session Handoff — 2026-06-17 (afternoon/evening)

## TL;DR
Big session: shipped social-login profile + account-linking hardening, fixed "profile edits don't save", upgraded all mobile apps to **Expo SDK 56 / RN 0.85**, removed Sentry (vendor crash), cleaned stale prod test accounts, made the repo **public**, deployed `auth-bff` + `api`. Most of it is on `main` + on TestFlight. **New rule going forward: feature-branch + PR to main, NO direct pushes** (see `feedback_feature_branch_pr_workflow` memory).

## DONE + verified
- **Social-login name + avatar** populate (backfill-only) via GIP claims — backend (auth-bff + api) deployed. Device-verified.
- **Account linking by email** (Google+Apple → one account) — hardened with `email_verified` gate. **Device-verified by owner.**
- **"Profile edits don't save"** — was a mobile read-side `{data}` envelope mismatch (`data?.data` → flat). Fixed, device-verified.
- **Apple first-auth name** → Firebase displayName (3 login screens).
- **#10 security-middleware tests** — 46 Go tests (force-upgrade/rate-limit/idempotency/webhook-HMAC), miniredis, green on main. Labeled `in review`.
- **#22 auth-bff registry cleanup** — dead web OIDC entries removed; deployed.
- **#9 closed** (iOS launch verified on TestFlight).
- **#66 keychain entitlement** — added to main; commented. (Was unsigned-sim-only; team-signed builds get default keychain.)
- **Repo made PUBLIC** (to get free GitHub Actions — org Actions billing was blocking all CI). Secret scan clean before flip. Auto-merge workflow PAUSED.
- **Expo SDK 55→56 upgrade** (3 apps + mobile-shared, RN 0.85): build-verified (#4 customer) AND runtime-verified on device.
- **Stale prod test accounts cleaned** (GKE `homechef-postgres`, db `homechef_db`): soft-deleted `e2e-test@fe3dr.com` + `priya.testcustomer@example.com`; blanked name/phone on `mahesh.sangawar@gmail.com` (owner's real account — re-backfills real name on next Google sign-in). 13 active / 2 soft-deleted.

## IN-FLIGHT / needs owner verification
- **Vendor build #6** (`8e5febc9`, Sentry-removed) FINISHED → TestFlight. **Install + confirm vendor app launches (no crash).**
- **Customer #5** + **vendor #6** on TestFlight carry SDK 56 + keychain + signup social-login (#71). Verify #66 **signup** path (email/pw signup reaches `/(tabs)`, no bounce).
- Owner's account: sign out/in (or edit profile in-app) so the blanked name backfills the real Google name + set phone.

## RESOLVED (transient) — vendor sign-in 502
**Update: worked on retry → it was a transient Knative cold-start 502 while the api revision settled. Vendor now loads (Sentry fix) + sign-in works. Monitor for recurrence; the error-code UX bug below is the real follow-up.**

### (incident detail, for reference)
- Vendor app now **loads** (Sentry fix confirmed). But sign-in → **502** on `POST /auth/auto-login` (auth-bff log 04:51:59 UTC, 248ms).
- **Evidence:** api `user-container` logged NO `/internal/users/upsert` at that timestamp → the BFF→api upsert call failed **before reaching the api handler** (fast fail, not a 5s timeout). NOT a DB issue: api reads work (`GET /chefs` 200), manual DB writes commit fine, primary `homechef-postgres-7` has disk space (1% used).
- **Concurrent noise:** api↔NATS flapping constantly ("connection reset by peer", reconnects every ~2min); CNPG `homechef-postgres` status "Not enough disk space" + `homechef-postgres-10` CrashLoopBackOff (replica). `homechef-api-00101` is a fresh Knative revision (~21min old at incident).
- **Leading hypotheses (UNVERIFIED):** (1) transient Knative cold-start/rollout 502 as api revision settled → RETRY first; (2) auth-bff→api connectivity/routing (check auth-bff's API base URL config + the api Knative service/route); (3) HMAC/BFFAuth rejecting before logging. **Next step:** reproduce + check auth-bff logs for the upstream error detail + confirm the upsert reaches api on retry. Likely transient — verify before deep-diving.

## UX BUG (confirmed, not yet fixed) — raw error codes shown to users
- Login screens show the **raw error** to users (e.g. "Sign-in failed: 502" / "auto_login_502"). `apps/mobile-vendor/app/(auth)/login.tsx:82-84` does `msg = err instanceof Error ? err.message : ...; Alert.alert('Sign-in failed', msg)` — it does NOT use the existing friendly mapper.
- **Fix:** `resolveAuthErrorMessage(err)` already exists in `packages/mobile-shared/src/auth/bff-session.ts` and maps every code (incl. `auto_login_502` → "We're having a hiccup on our end. Try again in a moment.") with a safe generic fallback. Import it and replace `err.message` in EVERY catch block (~4 in vendor; ~lines 81/106/120/146) in all THREE login screens (mobile-vendor, mobile-customer, mobile-delivery). Audit other user-facing `Alert.alert(..., err.message)` / `error.message` spots app-wide while at it — owner wants NO raw codes/messages surfaced to users ever.

## OPEN / next steps
1. ⚠️ **Prod DB health incident** (NOT yet addressed): `homechef-postgres` cluster = "Not enough disk space"; `homechef-postgres-10` CrashLoopBackOff (564 restarts). Primary `homechef-postgres-7` serving. Needs disk resize / cleanup before it rejects writes.
2. **lucide-react-native type errors** (~48, vendor; type-only, non-blocking — builds fine) from SDK 56 dep churn. Realign lucide types in a follow-up PR.
3. **Close redundant dependabot expo-* PRs** #79/#84/#85/#86/#87 (superseded by SDK 56 bump).
4. Sam manually merged 3 majors (firebase 12, sonner 2, hookform 5) — in vendor #6; watch during device testing.
5. Next backend issue candidate: **#12 DPDP export/delete tests** (same test-automation pattern as #10).

## Key context
- **Workflow: feature-branch + PR to main. Do NOT push to main directly.** (`feedback_feature_branch_pr_workflow`)
- **gh CLI:** use `gh auth switch --user mahesh-sangawar` for tesserix repo access (NOT the default Mahesh-Sangawar_civica).
- **Deploy:** push/merge to main → per-service CI (`.github/workflows/homechef-*-build.yml`) → GHCR → `TESSERIX_K8S_BOT` PAT bumps tag in `tesserix/tesserix-k8s` argocd manifest → ArgoCD. PAT expires → silent "Bad credentials" (refresh + SSO-authorize). (`reference_deploy_pipeline_and_repo_public`)
- **Prod DB access:** NOT Cloud SQL. Postgres in GKE cluster `tesseract-prod-in-gke` (asia-south1), namespace `homechef`, CNPG primary `homechef-postgres-7`, db `homechef_db`. Connect: `gcloud container clusters get-credentials tesseract-prod-in-gke --region asia-south1 --project tesseracthub-480811` then `kubectl exec -i homechef-postgres-7 -n homechef -c postgres -- psql -U postgres -d homechef_db`. **NOTE: `kubectl exec` needs `-i` to forward stdin/heredocs.** Active kubectl context was switched to this GKE cluster — switch back to your AKS work context if needed.
- Memories updated: `project_account_linking_by_email`, `project_social_login_profile_enrichment`, `reference_deploy_pipeline_and_repo_public`, `feedback_feature_branch_pr_workflow`, `project_ios_launch_crash_debug`.
