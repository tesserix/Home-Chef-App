---
phase: quick-260615-i1h
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web-landing/lib/site.ts
  - apps/web-landing/components/legal-page.tsx
  - apps/web-landing/app/privacy/page.tsx
  - apps/web-landing/app/terms/page.tsx
  - apps/web-landing/app/refund/page.tsx
  - apps/web-landing/app/vendor-terms/page.tsx
  - apps/web-landing/app/eula/page.tsx
  - apps/mobile-vendor/components/legal/LegalScreen.tsx
  - apps/mobile-vendor/app/privacy.tsx
  - apps/mobile-vendor/app/terms.tsx
  - apps/mobile-vendor/app/chef-agreement.tsx
  - apps/mobile-vendor/app/eula.tsx
  - apps/mobile-vendor/app/(tabs)/more.tsx
  - apps/mobile-vendor/locales/en.json
  - apps/mobile-vendor/locales/hi.json
  - apps/mobile-customer/app/privacy.tsx
  - apps/mobile-customer/app/terms.tsx
  - apps/mobile-customer/app/refund.tsx
  - apps/mobile-customer/app/eula.tsx
  - apps/mobile-customer/app/(tabs)/profile.tsx
  - apps/mobile-vendor/app/settings.tsx
  - COUNSEL-REVIEW.md
autonomous: true
requirements: [PROD-READINESS-W3-legal-urls, PROD-READINESS-W6-legal-review, PROD-READINESS-W5A-legal-stubs]

must_haves:
  truths:
    - "fe3dr.com/privacy, /terms, /refund render full multi-section policy bodies (not 1-line summaries)"
    - "fe3dr.com/vendor-terms renders the Chef/Vendor Agreement (the URL vendor onboarding already links to)"
    - "fe3dr.com/eula renders an End User Licence Agreement"
    - "Vendor app has Privacy, Terms, Chef/Vendor Agreement, and EULA screens reachable from the More tab"
    - "Customer app has an EULA screen reachable from Profile > Legal"
    - "Operator name 'Tesserix Pty Ltd', support email 'support@fe3dr.com', and date '11 June 2026' are identical across landing + customer + vendor legal surfaces"
    - "COUNSEL-REVIEW.md exists at repo root with the AU-entity-vs-India-operation conflict flagged as the top item"
  artifacts:
    - path: "apps/web-landing/app/privacy/page.tsx"
      provides: "Full privacy policy body fed into LegalPage"
      contains: "sections"
    - path: "apps/web-landing/app/vendor-terms/page.tsx"
      provides: "Chef/Vendor Agreement page (resolves the dangling vendor-onboarding link)"
    - path: "apps/web-landing/app/eula/page.tsx"
      provides: "EULA page"
    - path: "apps/mobile-vendor/components/legal/LegalScreen.tsx"
      provides: "Vendor StyleSheet-themed legal screen layout"
      contains: "LegalSection"
    - path: "apps/mobile-vendor/app/chef-agreement.tsx"
      provides: "Chef/Vendor Agreement screen (commission, payouts, FSSAI, cancellation duties)"
    - path: "COUNSEL-REVIEW.md"
      provides: "Structured lawyer-review checklist"
      contains: "Tesserix Pty Ltd"
  key_links:
    - from: "apps/mobile-vendor/app/(tabs)/more.tsx"
      to: "/privacy, /terms, /chef-agreement, /eula"
      via: "NAV_ROWS router.push rows under a Legal group"
      pattern: "chef-agreement"
    - from: "apps/mobile-customer/app/(tabs)/profile.tsx"
      to: "/eula"
      via: "Legal section row"
      pattern: "/eula"
    - from: "apps/web-landing/components/legal-page.tsx"
      to: "structured sections"
      via: "new optional sections prop"
      pattern: "sections"
---

<objective>
Finish PROD-READINESS point 2: bring every legal document to a consistent, complete, reviewer-ready state across the three live surfaces (fe3dr.com landing, customer app, vendor app). Goal is a package a lawyer can review cheaply in one pass — NOT legal sign-off.

Five deliverables:
1. Expand the 3 landing stubs (/privacy, /terms, /refund) into full structured policy bodies — these are the canonical URLs the apps link to.
2. Add the missing vendor-app legal screens: Privacy, Terms, a Chef/Vendor Agreement, and an EULA, wired into the More tab.
3. Add an EULA on landing (/eula) and in both apps (our own EULA; Apple's standard EULA is the noted fallback).
4. Consistency sweep: operator "Tesserix Pty Ltd", support "support@fe3dr.com", date "11 June 2026" — identical everywhere.
5. Write COUNSEL-REVIEW.md at repo root listing every decision counsel must confirm, with the AU-entity-vs-India-operation conflict as the top, prominently-flagged item.

Purpose: PROD-READINESS W3 ("Privacy policy + EULA URLs"), W6 ("Legal content review"), W5A (landing legal stubs) are the last legal gates before a counsel pass.
Output: 5 expanded/new landing pages, 1 vendor LegalScreen + 4 vendor screens + nav wiring, 1 EULA per app, a consistency sweep, and COUNSEL-REVIEW.md.

These are TEMPLATES pending counsel review. Each file carries a code-comment note ("template — have counsel review before launch"). Do NOT add alarming user-facing "this is not legal advice / draft" banners beyond what the customer app already does (the customer app shows none; match that restraint). Do NOT claim legal compliance as fact in user-facing copy beyond what the existing customer screens already state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

# Richest source prose — REUSE this content (do NOT edit these SPA files; they are SUNSET, source-only):
@apps/web/src/features/legal/pages/PrivacyPolicyPage.tsx
@apps/web/src/features/legal/pages/TermsPage.tsx
@apps/web/src/features/legal/pages/RefundPolicyPage.tsx

# Landing component + stubs to expand:
@apps/web-landing/components/legal-page.tsx
@apps/web-landing/lib/site.ts
@apps/web-landing/app/privacy/page.tsx

# Customer app pattern to MIRROR (already canonical: support@fe3dr.com, 11 June 2026):
@apps/mobile-customer/components/legal/LegalScreen.tsx
@apps/mobile-customer/app/privacy.tsx

# Vendor app — styling system + nav to wire into:
@apps/mobile-vendor/app/(tabs)/more.tsx

<interfaces>
<!-- Contracts the executor needs. No codebase exploration required. -->

Customer LegalScreen (NativeWind-based — DO NOT reuse verbatim in vendor; vendor has no className):
  interface LegalSection { heading: string; paragraphs: string[]; }  // "• " prefix → bullet line
  <LegalScreen title lastUpdated intro? sections />

Vendor styling reality (apps/mobile-vendor): StyleSheet only, NO NativeWind className.
  Theme: import { theme } from '@homechef/mobile-shared/theme'
  Tokens used by vendor screens: theme.colors.bone (canvas), theme.colors.paper (card),
    theme.colors.ink.DEFAULT / .soft / .muted (text), theme.colors.mist.DEFAULT (hairline),
    theme.colors.destructive.DEFAULT, theme.spacing[n], theme.radius.lg, theme.shadow[1],
    fonts: 'Geist-Bold' (28pt command title), 'Inter-SemiBold', 'Inter'.
  Back header pattern (copy from settings.tsx): SafeAreaView edges={['top','left','right']},
    commandBar row with ChevronLeft (router.back()) + Geist-Bold 28pt title.
  Routing: file-based <Stack> in app/_layout.tsx — adding app/X.tsx auto-registers route /X.
    No manual route table to edit.
  More tab (more.tsx): NAV_ROWS array of { labelKey, caption?, route, Icon };
    labels via t(`more.${labelKey}`); locales/en.json + hi.json hold the `more` namespace.

Landing LegalPage (apps/web-landing/components/legal-page.tsx) CURRENT signature:
  interface LegalPageProps { title: string; summary: string; }  // renders summary + "being finalised" placeholder
  Uses Tailwind tokens: font-display, text-charcoal, text-charcoal-soft, bg, max-w-3xl.
  Imports CONTACT_EMAIL from '@/lib/site' (currently 'hello@fe3dr.com').

Landing site.ts: SITE_URL='https://fe3dr.com'; CONTACT_EMAIL='hello@fe3dr.com'.
  Legal pages need support@fe3dr.com (per consistency rule) — add a LEGAL_SUPPORT_EMAIL constant,
  do NOT repurpose CONTACT_EMAIL (it is the general contact and used elsewhere).

Vendor onboarding already links to https://fe3dr.com/vendor-terms (apps/mobile-vendor/app/(onboarding)/policies.tsx)
  — that page does NOT exist yet. Task 1 must create it so the link resolves.
</interfaces>

# Canonical values for the consistency sweep (use EXACTLY these everywhere user-facing):
#   Operator:  Tesserix Pty Ltd
#   Support:   support@fe3dr.com
#   Date line: Last updated: 11 June 2026   (and "Effective: 11 June 2026" where a page shows both)
# Placeholders to leave as bracketed TODOs (counsel fills): registered address, CIN/ABN/ACN, GSTIN,
#   Grievance Officer name + phone. Mark each with a code comment "// TODO(counsel): ...".
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand the 3 landing stubs to full bodies + add /vendor-terms and /eula on landing</name>
  <files>apps/web-landing/lib/site.ts, apps/web-landing/components/legal-page.tsx, apps/web-landing/app/privacy/page.tsx, apps/web-landing/app/terms/page.tsx, apps/web-landing/app/refund/page.tsx, apps/web-landing/app/vendor-terms/page.tsx, apps/web-landing/app/eula/page.tsx</files>
  <action>
Make fe3dr.com/{privacy,terms,refund} canonical full policies, and add /vendor-terms (Chef/Vendor Agreement) + /eula.

1. site.ts: add `export const LEGAL_SUPPORT_EMAIL = 'support@fe3dr.com';` and `export const LEGAL_OPERATOR = 'Tesserix Pty Ltd';` and `export const LEGAL_LAST_UPDATED = '11 June 2026';`. Do NOT change CONTACT_EMAIL (general contact, used elsewhere).

2. legal-page.tsx: extend `LegalPageProps` to accept an optional structured body so the same component renders full content:
   - Add `lastUpdated?: string` and `sections?: LegalSection[]` where
     `interface LegalSection { heading: string; paragraphs: string[]; }` ("• " prefix → bullet `<li>`).
   - When `sections` is provided: render an intro/operator line ("Fe3dr is a product of {LEGAL_OPERATOR}."), a "Last updated: {lastUpdated}" line, then each section as `<h2>` + paragraphs/bullets. Keep `summary` as the lead paragraph. Drop the "being finalised … will be published before launch" placeholder text path when sections exist (it is now published).
   - Keep existing Tailwind tokens (font-display, text-charcoal, text-charcoal-soft, max-w-3xl, underline-offset-4). For the contact email link inside legal bodies use LEGAL_SUPPORT_EMAIL, not CONTACT_EMAIL.
   - Add a code comment at top: `// Template legal content — have counsel review before launch (see COUNSEL-REVIEW.md).`

3. privacy/page.tsx, terms/page.tsx, refund/page.tsx: replace the bare `summary` props with full `sections` arrays.
   - SOURCE THE PROSE from apps/web/src/features/legal/pages/{PrivacyPolicyPage,TermsPage,RefundPolicyPage}.tsx (the richest content). Lift the section text — but NORMALISE every value to the canonical set: operator "Tesserix Pty Ltd" with NO Australian ACN/ABN suffix in user-facing copy (the AU suffix goes to COUNSEL-REVIEW.md, not the live page), email "support@fe3dr.com" (the SPA uses support@homechef.in / grievance@homechef.in — replace ALL of them), and "Last updated: 11 June 2026".
   - Privacy: cover who-we-are, what-we-collect, why, who-we-share-with, retention, your rights (DPDP), security, children (18+), Grievance Officer (name/phone as TODO(counsel) brackets), changes/contact. Keep DPDP framing but do not invent new claims beyond the SPA source.
   - Terms: about Fe3dr (marketplace/intermediary), account/eligibility (18+), placing an order, payment (Razorpay/Stripe escrow), cancellations/refunds (cross-link /refund), food safety/allergens/FSSAI, reviews/content, prohibited use, liability cap, intermediary status, governing law (carry the SPA's India-operations framing; AU-vs-India conflict → COUNSEL-REVIEW.md), dispute resolution, privacy ref, changes, contact.
   - Refund: the order-stage cancellation matrix (render as a sections table or as bulleted stage rows), chef cancellations/force majeure, how-to-request, timeline (7 working days, RBI PA Master Direction), refund channel (original payment method), partial refunds, catering, chargebacks, payment-failure auto-reversal, non-refundable list, COD, grievance officer, changes.
   - Each page keeps its `metadata` block (title/description/canonical) unchanged.

4. vendor-terms/page.tsx (NEW): Chef/Vendor Agreement. This URL is ALREADY linked from vendor onboarding (apps/mobile-vendor/app/(onboarding)/policies.tsx → https://fe3dr.com/vendor-terms), so it must resolve. Use LegalPage with sections covering: who the agreement is between (chef ↔ Tesserix Pty Ltd), independent-contractor status (chef is not an employee), menu/pricing control, commission + platform fee model, payout schedule (Razorpay route/settlement, weekly), FSSAI registration + food-safety responsibility sits with the chef, accurate allergen/ingredient disclosure duty, order acceptance + cancellation duties (chef-initiated cancellation rules, force majeure), tax responsibility (chef's own GST/income tax), reviews/content licence, suspension/termination, liability, governing law, contact. metadata: title "Chef & Vendor Agreement", canonical '/vendor-terms/'.

5. eula/page.tsx (NEW): End User Licence Agreement. Use LegalPage with sections: licence grant (limited, non-exclusive, non-transferable, revocable licence to use the Fe3dr apps), ownership/IP (Tesserix Pty Ltd retains all rights), restrictions (no reverse-engineering, no resale, no automated scraping), third-party stores (Apple App Store / Google Play terms also apply; note "Apple's standard Licensed Application End User Licence Agreement applies as a fallback where this EULA is silent"), updates, termination, disclaimer of warranties, limitation of liability, governing law, contact (support@fe3dr.com). metadata: title "End User Licence Agreement", canonical '/eula/'.

All five page files plus vendor-terms and eula: add the top code comment "// Template legal content — have counsel review before launch (see COUNSEL-REVIEW.md)." Define the `LegalSection` type once (export from legal-page.tsx, import in pages).
  </action>
  <verify>
    <automated>cd apps/web-landing && npx tsc --noEmit 2>&1 | head -20 && grep -rL "11 June 2026" app/privacy/page.tsx app/terms/page.tsx app/refund/page.tsx | grep -q . && echo "MISSING DATE" || echo "date ok" && grep -rl "homechef.in" app/ && echo "DRIFT FOUND" || echo "no homechef.in drift"</automated>
  </verify>
  <done>tsc passes; /privacy /terms /refund /vendor-terms /eula each render multi-section bodies via LegalPage; operator/email/date are canonical; no "homechef.in" or AU ACN/ABN appears in user-facing landing copy; the /vendor-terms link from vendor onboarding now resolves.</done>
</task>

<task type="auto">
  <name>Task 2: Vendor LegalScreen + Privacy/Terms/Chef-Agreement/EULA screens + More-tab wiring; customer EULA</name>
  <files>apps/mobile-vendor/components/legal/LegalScreen.tsx, apps/mobile-vendor/app/privacy.tsx, apps/mobile-vendor/app/terms.tsx, apps/mobile-vendor/app/chef-agreement.tsx, apps/mobile-vendor/app/eula.tsx, apps/mobile-vendor/app/(tabs)/more.tsx, apps/mobile-vendor/locales/en.json, apps/mobile-vendor/locales/hi.json, apps/mobile-customer/app/eula.tsx, apps/mobile-customer/app/(tabs)/profile.tsx</files>
  <action>
Vendor app has NO legal screens and NO NativeWind (StyleSheet + theme only), so MIRROR the customer LegalScreen pattern but render it with the vendor's StyleSheet/theme system — do NOT import the customer's NativeWind component.

1. apps/mobile-vendor/components/legal/LegalScreen.tsx (NEW): same prop contract as customer
   (`interface LegalSection { heading: string; paragraphs: string[] }`, "• " prefix → bullet line;
   props: title, lastUpdated, intro?, sections), but built with StyleSheet + `theme` from
   '@homechef/mobile-shared/theme'. Header = command bar copied from settings.tsx (SafeAreaView
   edges top/left/right, ChevronLeft → router.back(), Geist-Bold 28pt title, bone canvas). Body =
   ScrollView with "Last updated: {lastUpdated}", optional intro, then sections (Inter-SemiBold
   heading + Inter ink.soft paragraphs, bullets indented). Top comment: "// Template legal content — have counsel review before launch (see COUNSEL-REVIEW.md)."

2. apps/mobile-vendor/app/privacy.tsx + terms.tsx (NEW): vendor-facing Privacy + Terms.
   Reuse/adapt the customer app's content (apps/mobile-customer/app/{privacy,terms}.tsx) but in the
   VENDOR voice (the user is the chef). Same canonical values (operator "Tesserix Pty Ltd",
   support@fe3dr.com, LAST_UPDATED = '11 June 2026'). Each renders <LegalScreen .../> from the new
   vendor component. Privacy: what chef data we collect (profile, kitchen, FSSAI docs, payout/bank,
   device), why, sharing (customers see chef name/kitchen; Razorpay for payouts), retention, rights,
   contact. Terms: chef's relationship with the platform — but for the full commercial terms point to
   the Chef/Vendor Agreement screen.

3. apps/mobile-vendor/app/chef-agreement.tsx (NEW): the Chef/Vendor Agreement, same section content
   as landing /vendor-terms (Task 1.4) — independent-contractor status, commission + platform fee,
   payout schedule, FSSAI/food-safety responsibility on the chef, allergen-disclosure duty, order
   acceptance + cancellation duties, chef's own tax responsibility, suspension/termination, liability,
   governing law, contact. Render via <LegalScreen title="Chef & Vendor Agreement" .../>.

4. apps/mobile-vendor/app/eula.tsx (NEW) and apps/mobile-customer/app/eula.tsx (NEW): EULA mirroring
   landing /eula content (Task 1.5). Customer eula.tsx uses the EXISTING customer LegalScreen
   (NativeWind) at apps/mobile-customer/components/legal/LegalScreen.tsx; vendor eula.tsx uses the NEW
   vendor LegalScreen. Both: LAST_UPDATED '11 June 2026', operator "Tesserix Pty Ltd",
   support@fe3dr.com, and the "Apple standard EULA applies as a fallback" note.

5. Wire vendor More tab (apps/mobile-vendor/app/(tabs)/more.tsx): add a Legal group to NAV_ROWS —
   rows for privacy ('/privacy'), terms ('/terms'), chef agreement ('/chef-agreement'), eula ('/eula').
   Use an appropriate lucide icon already imported or add one (e.g. FileText/Scale/ShieldCheck).
   Add labelKeys (legalPrivacy, legalTerms, legalAgreement, legalEula) to the `more` namespace in
   BOTH locales/en.json and locales/hi.json. Keep captions short. Routes auto-register via the
   file-based Stack — no _layout edit needed.

6. Wire customer Profile Legal section (apps/mobile-customer/app/(tabs)/profile.tsx): add a 4th row
   "End User Licence" → router.push('/eula') beneath the existing Terms/Privacy/Refund rows.

Do not add alarming user-facing draft banners — match the customer app's restraint (it shows none).
  </action>
  <verify>
    <automated>cd apps/mobile-vendor && npx tsc --noEmit 2>&1 | head -20; cd ../mobile-customer && npx tsc --noEmit 2>&1 | head -20; cd ../.. && grep -q "chef-agreement" apps/mobile-vendor/app/\(tabs\)/more.tsx && echo "more wired" || echo "MORE NOT WIRED"; grep -q "/eula" apps/mobile-customer/app/\(tabs\)/profile.tsx && echo "profile wired" || echo "PROFILE NOT WIRED"</automated>
  </verify>
  <done>Both apps typecheck. Vendor app has privacy/terms/chef-agreement/eula screens via a vendor StyleSheet LegalScreen, all reachable from a new More-tab Legal group (labels in en+hi). Customer app has an EULA screen reachable from Profile > Legal. All screens use operator "Tesserix Pty Ltd", support@fe3dr.com, date "11 June 2026".</done>
</task>

<task type="auto">
  <name>Task 3: Consistency sweep + write COUNSEL-REVIEW.md at repo root</name>
  <files>apps/mobile-vendor/app/settings.tsx, COUNSEL-REVIEW.md</files>
  <action>
1. Consistency-sweep fixes (drift found during planning):
   - apps/mobile-vendor/app/settings.tsx line ~283: the delete-account alert says
     "Email support@homechef.app to request account deletion." Change to "support@fe3dr.com".
   - Re-grep all three live surfaces for residual drift and fix any user-facing occurrence:
     run `grep -rn "homechef.in\|homechef.app\|grievance@homechef\|legal@homechef\|13 May 2026\|hello@fe3dr" apps/mobile-customer apps/mobile-vendor apps/web-landing --include=*.tsx --include=*.ts --include=*.json` and replace user-facing legal-contact occurrences with support@fe3dr.com / 11 June 2026.
     EXCLUDE: apps/web/** (sunset SPA — source only, do NOT edit), and site.ts CONTACT_EMAIL/CHEFS_EMAIL/hello@ (those are general/marketing contacts, not legal-doc support — leave them). Note any drift you deliberately leave (e.g. the SPA's stale values) in COUNSEL-REVIEW.md.
   - Confirm operator string is exactly "Tesserix Pty Ltd" (no ", Australia" / ACN / ABN) in all user-facing app + landing legal copy.

2. Write COUNSEL-REVIEW.md at the REPO ROOT
   (/Users/Mahesh.Sangawar/personal/tesserix-new/Home-Chef-App/COUNSEL-REVIEW.md).
   Structured Markdown checklist (each item a `- [ ]` line with a short why + where-it-appears).
   Sections in this order:

   ## ⚠ TOP PRIORITY — entity vs jurisdiction conflict (must resolve before launch)
     Flag prominently: operator is "Tesserix Pty Ltd" — an Australian entity suffix (the sunset
     web SPA even carries ACN 694 070 865 / ABN 59 694 070 865, NSW Australia) — yet every policy
     asserts India-only operation: compliance with India's DPDP Act 2023, Indian GST invoicing,
     an Indian-style Grievance Officer (DPDP §10/§13, IT Intermediary Rules 2021), RBI Payment
     Aggregator refund timelines, and FSSAI food-safety law. List the specific mismatches counsel
     must resolve: (a) governing law / jurisdiction clause (AU vs India — Terms §12 of SPA shows a
     split clause), (b) which legal entity is the DPDP "Data Fiduciary", (c) GST registration /
     GSTIN of the operating entity, (d) whether an Indian subsidiary or the AU entity contracts
     with chefs and customers, (e) Grievance Officer must be an India-resident appointee.

   ## Refund & cancellation vs Razorpay
     Confirm 7-working-day refund window + original-payment-method rule + escrow/settlement wording
     match the actual Razorpay/RBI PA Master Direction obligations and the live refund implementation
     (PROD-READINESS W3 cancel/refund endpoints).

   ## Data protection / DPDP
     Confirm: data-retention periods (the SPA asserts 8y order records, 3y FSSAI, 90-day deletion,
     etc.), cross-border transfer wording (Stripe US), breach-notification commitment, children/18+
     gate, consent + withdrawal flow, Grievance Officer 15-day response.

   ## Marketplace / liability allocation
     Confirm intermediary status (IT Act §79), chef independent-contractor + FSSAI/food-safety
     liability allocation, delivery-partner (3PL) liability allocation, allergen-disclosure liability,
     review/defamation wording, liability cap (= platform fee per order).

   ## EULA
     Confirm our EULA is acceptable to Apple/Google or whether to fall back to Apple's standard
     Licensed Application EULA; confirm IP ownership entity.

   ## Outstanding placeholders (real values needed before launch)
     - [ ] Registered address of the operating entity
     - [ ] CIN (if Indian Pvt Ltd) and/or ACN/ABN (if AU) — single authoritative entity
     - [ ] GSTIN
     - [ ] Grievance Officer name + phone (currently bracketed TODO(counsel))
     - [ ] Confirm support email support@fe3dr.com is monitored / contractually named

   ## Known drift deliberately left
     Note that apps/web/** (sunset SPA) still carries 13 May 2026 + homechef.in emails + the AU
     ACN/ABN — left untouched because the web apps are being sunset (app-only). Listed here only so
     counsel isn't surprised if they grep the repo.

   Add a one-line header note: "Status: template legal package, reviewer-ready. NOT legal sign-off.
   Generated 15 June 2026."
  </action>
  <verify>
    <automated>test -f COUNSEL-REVIEW.md && grep -q "Tesserix Pty Ltd" COUNSEL-REVIEW.md && grep -qi "TOP PRIORITY" COUNSEL-REVIEW.md && echo "counsel-review ok"; grep -rn "homechef.app\|grievance@homechef" apps/mobile-customer apps/mobile-vendor apps/web-landing --include=*.tsx --include=*.ts --include=*.json | grep -v node_modules && echo "RESIDUAL DRIFT" || echo "no residual drift in apps"</automated>
  </verify>
  <done>settings.tsx uses support@fe3dr.com; no homechef.in/homechef.app/grievance@homechef drift remains in the three live surfaces (SPA untouched); COUNSEL-REVIEW.md exists at repo root with the AU-vs-India conflict as the prominently flagged top item plus all required checklist sections.</done>
</task>

</tasks>

<verification>
- `cd apps/web-landing && npx tsc --noEmit` passes; /privacy /terms /refund /vendor-terms /eula render full bodies.
- `cd apps/mobile-vendor && npx tsc --noEmit` and `cd apps/mobile-customer && npx tsc --noEmit` pass.
- `grep -rn "Tesserix Pty Ltd" apps/web-landing apps/mobile-customer apps/mobile-vendor` shows consistent operator string; no ACN/ABN in user-facing copy.
- `grep -rn "support@fe3dr.com" ...` is the only legal support email; no homechef.in / homechef.app / grievance@homechef in the three live surfaces.
- `grep -rn "11 June 2026" ...` is the single date used across legal surfaces.
- COUNSEL-REVIEW.md exists at repo root with the entity/jurisdiction conflict flagged as item 1.
- Vendor More tab shows Privacy / Terms / Chef Agreement / EULA; customer Profile > Legal shows EULA.
</verification>

<success_criteria>
- All 5 scope items covered: (1) landing stubs expanded, (2) vendor legal screens added + wired, (3) EULAs (landing + both apps), (4) consistency sweep done, (5) COUNSEL-REVIEW.md written.
- No new heavy dependencies; existing components/patterns reused (landing LegalPage extended, customer LegalScreen reused, vendor LegalScreen mirrors the pattern in the vendor StyleSheet/theme).
- The dangling https://fe3dr.com/vendor-terms link from vendor onboarding now resolves.
- Every legal file carries a "template — have counsel review" code comment (not a user-facing alarm).
- All three apps typecheck; routes render.
</success_criteria>

<output>
After completion, create `.planning/quick/260615-i1h-finish-prod-readiness-point-2-legal-docs/260615-i1h-SUMMARY.md`
</output>
