---
phase: quick
plan: 260612-n0g
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web-landing/public/homechef-logo.svg
  - apps/web-landing/components/wordmark.tsx
  - apps/mobile-customer/assets/adaptive-icon.png
  - apps/mobile-customer/assets/splash-icon.png
  - apps/mobile-customer/app.json
  - apps/mobile-vendor/assets/adaptive-icon.png
  - apps/mobile-vendor/assets/splash-icon.png
  - apps/mobile-vendor/app.json
  - docs/ops/CUTOVER-RUNBOOK.md
  - PROD-READINESS.md
autonomous: true
requirements: [QUICK-POLISH-SVG, QUICK-POLISH-WORDMARK, QUICK-POLISH-MOBILE-ASSETS, QUICK-POLISH-DOC-DRIFT]

must_haves:
  truths:
    - "apps/web-landing/public/homechef-logo.svg exists and rasterizes cleanly via magick (gradient rounded-rect + white outline bowl + house + 3 steam waves)"
    - "Wordmark component renders the new bowl+house+steam mark (gradient bg, outline style) at 28px with unchanged { inverted } API and Home Chef logotype"
    - "Both apps' adaptive-icon.png are 1024x1024 WITH alpha: white glyph only, transparent background, glyph in inner ~60%"
    - "Both app.json files have adaptiveIcon.backgroundColor set to the mid-gradient coral and an expo-splash-screen plugin config with splash-icon.png + brand backgroundColor"
    - "CUTOVER-RUNBOOK §1 boxes at former lines 82-85 are [x] with DONE annotations; the wrong release-tag claim is corrected"
    - "PROD-READINESS 5B infra-prereq annotated DONE, Delivery 3PL item flipped [~]->[x], 7E checkbox flipped [ ]->[x]"
  artifacts:
    - path: "apps/web-landing/public/homechef-logo.svg"
      provides: "Vector source of truth for the new mark (full icon: gradient rounded rect + glyph)"
    - path: "apps/web-landing/components/wordmark.tsx"
      provides: "New-mark glyph inlined as SVG paths sharing geometry with homechef-logo.svg"
    - path: "apps/mobile-customer/assets/adaptive-icon.png"
      provides: "Transparent-foreground Android adaptive icon (white glyph, RGBA)"
    - path: "apps/mobile-vendor/assets/adaptive-icon.png"
      provides: "Transparent-foreground Android adaptive icon (white glyph, RGBA)"
  key_links:
    - from: "apps/web-landing/components/wordmark.tsx"
      to: "apps/web-landing/public/homechef-logo.svg"
      via: "shared glyph path geometry (same d= path data, glyph variant)"
      pattern: "stroke=\"#fff\""
    - from: "apps/mobile-customer/app.json"
      to: "apps/mobile-customer/assets/splash-icon.png"
      via: "expo-splash-screen plugin config"
      pattern: "expo-splash-screen"
---

<objective>
Polish batch, 4 items in one pass: (1) hand-author a vector SVG of the new coral bowl+house+steam Home Chef mark; (2) swap the web-landing Wordmark component to that mark; (3) fix Android adaptive icons (transparent foreground) + add splash screens for mobile-customer and mobile-vendor; (4) sync stale checkboxes in CUTOVER-RUNBOOK.md §1 and PROD-READINESS.md 5B/7E to shipped reality.

Purpose: brand consistency landing↔mobile, correct Android adaptive-icon rendering (current foreground has the gradient baked in solid — Android masks it badly), and docs that match what actually shipped.
Output: homechef-logo.svg, updated wordmark.tsx, 4 regenerated PNGs, 2 updated app.json, 2 corrected docs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@apps/web-landing/components/wordmark.tsx
@apps/web-landing/public/homechef-logo-source.png

**Verified facts (do not re-derive):**
- Source art: 1024x1024 RGB (no alpha), rounded-square icon, vertical gradient sampled at: top `#F34055`, mid `#FA6F5C`, bottom `#FAA15D`. Corners are opaque white. White OUTLINE bowl (wide rim line + bowl body) containing a white outline house (pitched roof + chimney) docked at the bowl rim, 3 wavy white steam strokes above.
- Current wordmark.tsx: 64x64 viewBox, flat `#FF385C` rect rx=14, FILLED white bowl + 2 steam drops, no house. Component API: `{ inverted }`, 28px SVG, "Home Chef" Geist logotype span. Keep all of that except the SVG contents.
- Mobile assets: both `apps/mobile-customer/assets/` and `apps/mobile-vendor/assets/` have `icon.png` (1024 RGB, correct, DO NOT TOUCH), `adaptive-icon.png` (1024 RGB solid — the problem), `splash-icon.png` (1x1 placeholder).
- Both app.json: `android.adaptiveIcon = { foregroundImage: "./assets/adaptive-icon.png", backgroundColor: "#ffffff" }`. NO splash config and NO expo-splash-screen plugin anywhere. Expo SDK `~55.0.26`, monorepo uses pnpm.
- Tools: `magick` at /opt/homebrew/bin/magick WITH the `rsvg-convert` SVG delegate (high-quality SVG rasterization confirmed). Python PIL 11.3.0 available as fallback.
- Doc line anchors (verify with grep before editing — may have drifted ±a few lines): CUTOVER-RUNBOOK.md unchecked §1 boxes at lines 82 (ExternalSecret), 83 (flip database.homechef.enabled), 84 (apps-registry row), 85 (release tag — text is WRONG). PROD-READINESS.md: "Infra prereq: grant a homechef_platform_admin DB role" ~line 272 (sub-bullet of the [x] chef-payout item), "[~] **Delivery (3PL) admin in tesserix-home**" ~line 275, 7E "- [ ] Manage delivery providers" ~line 341.
- Git: single-line commit messages, no signatures, targeted `git add <paths>` only (never -a/-A).
- Scope guard: do NOT touch tesserix-home or tesserix-k8s repos — docs in THIS repo only.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author homechef-logo.svg and swap the Wordmark glyph</name>
  <files>apps/web-landing/public/homechef-logo.svg, apps/web-landing/components/wordmark.tsx</files>
  <action>
1. Read apps/web-landing/public/homechef-logo-source.png with the Read tool to see the exact art (proportions of bowl, house, steam).
2. Hand-author `apps/web-landing/public/homechef-logo.svg` as a clean vector recreation, viewBox="0 0 64 64":
   - `<rect width="64" height="64" rx="14">` filled with a `<linearGradient>` (x1=0 y1=0 x2=0 y2=1): stop 0 `#F34055`, stop 1 `#FAA15D` (optionally a mid stop `#FA6F5C` at ~50% if it matches the source better).
   - Glyph as STROKED paths, `stroke="#fff"`, `fill="none"`, `stroke-linecap="round"`, `stroke-linejoin="round"`, stroke-width ~3–3.5 (in 64-unit space): bowl rim (wide horizontal line/shallow ellipse), bowl body (downward arc closing under the rim), house docked at the rim (pitched roof + small chimney, simple outline), 3 wavy steam strokes above the rim.
   - Group the glyph paths in a `<g id="glyph">` so the geometry is copy-pastable.
3. Verify visually: `magick -background none apps/web-landing/public/homechef-logo.svg -resize 512x512 /tmp/svg-check.png` then `magick apps/web-landing/public/homechef-logo-source.png -resize 512x512 /tmp/src-check.png` and Read both images side by side. Iterate paths until the recreation clearly matches the source (same composition: bowl+house+steam, outline style, gradient direction). Pixel-perfection NOT required; faithful composition IS.
4. Rewrite the SVG inside `apps/web-landing/components/wordmark.tsx` to inline the SAME gradient rect + glyph paths (copy the d= data from the authored SVG; give the linearGradient a unique id like `hc-wm-grad` to avoid DOM id collisions). Keep everything else identical: 28x28 render size, viewBox 0 0 64 64, aria-hidden, `{ inverted }` prop, logotype span and classes. Update the JSDoc comment (it says "steaming-bowl glyph" — now bowl+house+steam).
5. Type-check: `cd apps/web-landing && npx tsc --noEmit` (if the app has no tsconfig-driven check available, fall back to `npx next lint` or at minimum confirm the file parses via the build's tsc).
6. Commit: `git add apps/web-landing/public/homechef-logo.svg apps/web-landing/components/wordmark.tsx && git commit -m "feat(web-landing): new bowl+house+steam wordmark + vector SVG logo source"`
  </action>
  <verify>
    <automated>magick -background none apps/web-landing/public/homechef-logo.svg -resize 256x256 /tmp/verify-svg.png && magick identify /tmp/verify-svg.png && cd apps/web-landing && npx tsc --noEmit</automated>
  </verify>
  <done>homechef-logo.svg exists, rasterizes without error, and visually matches the source composition (verified by Reading the rendered PNG); wordmark.tsx inlines the new glyph with unchanged component API; tsc passes.</done>
</task>

<task type="auto">
  <name>Task 2: Transparent adaptive-icon foregrounds + splash screens for both mobile apps</name>
  <files>apps/mobile-customer/assets/adaptive-icon.png, apps/mobile-customer/assets/splash-icon.png, apps/mobile-customer/app.json, apps/mobile-vendor/assets/adaptive-icon.png, apps/mobile-vendor/assets/splash-icon.png, apps/mobile-vendor/app.json</files>
  <action>
Derive all assets from the SVG authored in Task 1 (single vector source of truth).

1. Create a temp glyph-only SVG at /tmp/hc-glyph.svg: viewBox="0 0 1024 1024", NO background rect, the `#glyph` paths from homechef-logo.svg scaled/translated so the glyph occupies the CENTER ~60% of the canvas (e.g. wrap in `<g transform="translate(204.8 204.8) scale(9.6)">` for 64→614.4 units; adjust translate so it's optically centered). White strokes, scale stroke-width proportionally.
2. Rasterize the adaptive-icon foreground (same file for both apps):
   `magick -background none /tmp/hc-glyph.svg -resize 1024x1024 /tmp/adaptive-icon.png`
   Verify RGBA + transparency: `magick identify -format "%[channels] %wx%h alpha:%[pixel:p{5,5}]\n" /tmp/adaptive-icon.png` must show an alpha channel (srgba) and a fully transparent corner pixel. Read the PNG to confirm: white glyph only, no rounded rect, comfortably inside the inner safe zone.
   Copy to BOTH `apps/mobile-customer/assets/adaptive-icon.png` and `apps/mobile-vendor/assets/adaptive-icon.png`.
3. Splash icon (same file for both apps): the glyph reads white-on-coral, so reuse the transparent white-glyph render at a sane size:
   `magick -background none /tmp/hc-glyph.svg -resize 512x512 /tmp/splash-icon.png` → copy to both apps' `assets/splash-icon.png` (replacing the 1x1 placeholders).
4. Update BOTH app.json files (Edit tool, preserve all existing content):
   a. `android.adaptiveIcon.backgroundColor`: `"#ffffff"` → `"#FA6F5C"` (mid-gradient coral, sampled — Android adaptive backgrounds cannot be gradients).
   b. Add to the `plugins` array:
      ```json
      ["expo-splash-screen", {
        "image": "./assets/splash-icon.png",
        "imageWidth": 200,
        "resizeMode": "contain",
        "backgroundColor": "#FA6F5C"
      }]
      ```
      This is the Expo SDK 52+ convention (config plugin, not the legacy `expo.splash` key).
5. Ensure expo-splash-screen is a direct dependency in each app (the config plugin requires it resolvable): check `ls apps/mobile-customer/node_modules/expo-splash-screen 2>/dev/null || ls node_modules/expo-splash-screen 2>/dev/null`. If missing, run `npx expo install expo-splash-screen` inside each app dir (pnpm monorepo — expo install handles it). If already hoisted/present (it ships with expo-router), just add it to each app's package.json dependencies with the version expo install would pick.
6. Sanity-check config: `cd apps/mobile-customer && npx expo config --type public > /dev/null` and same for mobile-vendor — must exit 0 (catches plugin resolution failures).
7. Commit: `git add apps/mobile-customer/assets/adaptive-icon.png apps/mobile-customer/assets/splash-icon.png apps/mobile-customer/app.json apps/mobile-customer/package.json apps/mobile-vendor/assets/adaptive-icon.png apps/mobile-vendor/assets/splash-icon.png apps/mobile-vendor/app.json apps/mobile-vendor/package.json` (plus lockfile if changed) `&& git commit -m "feat(mobile): transparent adaptive-icon foregrounds + branded splash screens for customer + vendor"`

Note in the SUMMARY: splash/adaptive-icon changes require a native rebuild (prebuild/EAS) to appear on device — no rebuild in this task.
  </action>
  <verify>
    <automated>magick identify -format "%f %[channels] %wx%h\n" apps/mobile-customer/assets/adaptive-icon.png apps/mobile-vendor/assets/adaptive-icon.png apps/mobile-customer/assets/splash-icon.png apps/mobile-vendor/assets/splash-icon.png | grep -c srgba | grep -q 4 && grep -q expo-splash-screen apps/mobile-customer/app.json && grep -q expo-splash-screen apps/mobile-vendor/app.json && grep -q '"#FA6F5C"' apps/mobile-customer/app.json</automated>
  </verify>
  <done>All 4 PNGs have alpha channels (adaptive icons 1024x1024 transparent-bg white glyph at ~60%, splash icons 512x512); both app.json have adaptiveIcon.backgroundColor #FA6F5C and the expo-splash-screen plugin block; `expo config` resolves cleanly for both apps.</done>
</task>

<task type="auto">
  <name>Task 3: Sync CUTOVER-RUNBOOK §1 and PROD-READINESS 5B/7E to shipped reality</name>
  <files>docs/ops/CUTOVER-RUNBOOK.md, PROD-READINESS.md</files>
  <action>
Edits in THIS repo only (tesserix-home/tesserix-k8s belong to other sessions). Grep for the anchor text first — line numbers are approximate.

**docs/ops/CUTOVER-RUNBOOK.md §1** (all provisioning verified live 2026-06-12, chefs:2 orders:6):
1. ExternalSecret box (~line 82): `[ ]` → `[x]`, append: ` **DONE 2026-06-12** — added in tesserix-k8s \`external-secrets/prod/tesserix/externalsecret.yaml\`.`
2. Flip-the-switch box (~line 83): `[ ]` → `[x]`, append: ` **DONE 2026-06-12** — \`database.homechef.enabled: true\` in \`charts/apps/company/values.yaml\`.`
3. apps-registry row box (~line 84): `[ ]` → `[x]`, append: ` **DONE 2026-06-12** — row inserted into \`tesserix_admin.apps\`.`
4. Release-tag box (~line 85): the claim "push-to-main alone does not deploy it" is WRONG. Rewrite the line to: `- [x] ~~Cut a tesserix-home release tag~~ **Not needed — corrected 2026-06-12:** tesserix-home deploys via \`ci.yml\` on push to \`main\` (build + set image SHA); \`release.yml\` only tags GHCR on \`v*.*.*\` tags. The homechef admin pages shipped with the normal main-branch deploy.`
Also note nearby (if §1 has an intro/status line): GCP secret `prod-homechef-platform-admin-password` + `homechef_platform_admin` role/grants + istio egress/NetPol were already `[x]` — leave those untouched.

**PROD-READINESS.md Wave 5B:**
5. In the chef-payout sub-bullet (~line 272), find the sentence starting `**Infra prereq:** grant a \`homechef_platform_admin\` DB role` — append after it: ` **DONE 2026-06-12** (role + grants live, \`HOMECHEF_DB_*\` wired via ExternalSecret + \`database.homechef.enabled: true\`).`
6. `- [~] **Delivery (3PL) admin in tesserix-home**` (~line 275): the text already says DONE — flip `[~]` → `[x]`. No text changes.

**PROD-READINESS.md Wave 7E:**
7. `- [ ] Manage delivery providers (config/keys/toggle/test)...` (~line 341): `[ ]` → `[x]`, append: ` **DONE 2026-06-12** (tesserix-home \`c10d9aa\`: provider list/toggle + cost reconciliation; key-config + test-connection intentionally stay in homechef-api).`

8. Commit: `git add docs/ops/CUTOVER-RUNBOOK.md PROD-READINESS.md && git commit -m "docs: sync CUTOVER-RUNBOOK §1 + PROD-READINESS 5B/7E checkboxes to shipped reality"`
  </action>
  <verify>
    <automated>! grep -qE '^\- \[ \].*(ExternalSecret pointing at|Flip the switch|tesserix_admin\.apps|tesserix-home release tag)' docs/ops/CUTOVER-RUNBOOK.md && ! grep -q '\[~\] \*\*Delivery (3PL) admin' PROD-READINESS.md && grep -q '\[x\] Manage delivery providers' PROD-READINESS.md</automated>
  </verify>
  <done>All four §1 runbook boxes are [x] with DONE/corrected annotations, the wrong release-tag claim is rewritten, PROD-READINESS infra-prereq is annotated DONE, Delivery 3PL item is [x], and 7E is [x] with the c10d9aa annotation. Nothing outside this repo touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Static brand assets, app config, and internal docs — no untrusted input paths added |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-01 | T (Tampering) | app.json plugin config | accept | Config reviewed in diff; `expo config` validation catches malformed plugin entries |
</threat_model>

<verification>
1. `magick -background none apps/web-landing/public/homechef-logo.svg -resize 256x256 /tmp/final-check.png` exits 0; Read shows gradient rounded-rect + outline bowl/house/steam matching the source PNG composition.
2. `grep -c 'stroke="#fff"' apps/web-landing/components/wordmark.tsx` ≥ 1 and the old filled-bowl path (`d="M14 34h36`) is gone.
3. All 4 mobile PNGs report `srgba`; both adaptive icons are 1024x1024 with transparent corners.
4. Both app.json contain the expo-splash-screen plugin block and `#FA6F5C`; `npx expo config --type public` exits 0 in both app dirs.
5. Doc greps in Task 3 verify pass; `git log --oneline -3` shows the three single-line commits.
</verification>

<success_criteria>
- New vector logo committed at apps/web-landing/public/homechef-logo.svg, visually faithful to homechef-logo-source.png
- Wordmark component shows the new mark with identical API/size/logotype
- Android adaptive icons are proper transparent foregrounds with brand backgroundColor; splash screens configured per Expo SDK 55 conventions in both mobile apps
- CUTOVER-RUNBOOK §1 and PROD-READINESS 5B/7E reflect shipped reality with dated annotations; no edits outside this repo
</success_criteria>

<output>
After completion, create `.planning/quick/260612-n0g-polish-batch-landing-wordmark-swap-mobil/260612-n0g-SUMMARY.md`
</output>
