# HANDOFF 2026-07-01 — Fe3dr (Home Chef) v1 launch prep

## TL;DR
Drove v1 launch-scoping for the customer + vendor apps. Three PRs open (all awaiting the owner's merge), all tsc/go-test clean. The customer app is built + running on the iPhone 17 Pro sim **against the prod backend**, and the #371 changes are verified on-device. v1 engineering is essentially done; the remaining launch blockers are 4 owner-gated operational P0s. Borzo 3PL still parked on the owner's test token.

## OPEN PRs (owner authorizes merges — do NOT self-merge)
- **#369** `feat/chef-only-delivery-gating` — chef-only robust fulfillment. Gates delivery/rider on an enabled 3PL provider so no unfulfillable order can be created while 3PL is dark. Backend (`resolveFulfillment`/`resolveReadyCarrier` guards, `services.ThirdPartyDeliveryEnabled`, `offersDelivery` on chef resp, `riderDispatchAvailable` on order detail) + customer checkout + vendor Mark-Ready. Tests added.
- **#370** `feat/weekly-menu-scheduling` — à-la-carte weekly menu. Chef sets `MenuItem.availableDays` (0=Sun..6=Sat, empty=every day) once; `GetChefMenu`+`CreateOrder` filter to today's IST weekday (`services.MenuScheduleClause`/`TodayWeekday`); vendor menu editor "Available days" chips. **NOT the tiffin subscription** — see [[project_weekly_menu_scheduling]].
- **#371** `feat/v1-hide-tiffin-subscription` — hides ALL deferred/unimplemented customer surfaces behind reversible flags in `apps/mobile-customer/lib/features.ts` (all `false`): tiffin book/subscribe + grid, group orders, catering (profile row + home chip), wallet, rewards, refer & earn, social feed (profile MORE rows + home chip). Profile MORE section is now flag-filtered and drops out entirely when empty. Backend untouched (dark). Verified live on sim.
- Already MERGED earlier: **#359** (TestFlight builds fix + EAS Workflow CI).

Other branches: `feat/borzo-3pl-adapter` (Borzo, awaiting token), `integration/v1-sim-preview` (LOCAL only — main + #369+#370+#371 merged, used for the sim build; not pushed).

## v1 launch status (MVP milestone)
Engineering ~done. Remaining are **4 owner-gated P0s** (I can't complete these):
- **#25** Razorpay live switch — swap `rzp_test_*`→`rzp_live_*` in `homechef-api-secrets`, register live webhook at `api.fe3dr.com/webhooks/razorpay`, smoke a ₹ txn. (#6 test happy-path is CLOSED, so unblocked.)
- **#14** APNs prod cert — Apple key → Firebase + EAS → verify a push on TestFlight.
- **#19** Legal counsel sign-off — fill GSTIN / Grievance Officer / dpo@fe3dr.com; policy bodies are code-done.
- **#20** App Store submit — screenshots + IN listing copy (both apps), submit for review. Pipeline is zero-touch (tag `customer-v*`/`vendor-v*` or `eas workflow:run`).
Optional engineering (I can drive): **#8** full E2E order-lifecycle test, **#11** a11y + bundle audit. Post-launch: **#17** Cloudflare WAF.

Config verified OK for chef-only launch: delivery zones OFF (`HasActiveZones` false), 3PL providers dark (`is_enabled=false`), all money-flow flags default OFF (`WALLET_CHECKOUT`/`MEAL_PLAN_ESCROW`/`GROUP_ORDERS`/`CATERING_DEPOSIT`/`ORDER_SAGA`/`ONBOARDING_WORKFLOW`), Razorpay keys secret-driven, force-upgrade env-driven.

## SIM setup (still live) — customer app on prod
- **iPhone 17 Pro sim** UDID `9F9E4972-E434-47CD-B7DA-8BE294D41B97` (booted). Customer app built from source + installed, `.env.local` already points at PROD (`https://fe3dr.com/api`, real GIP/Google, `GoogleService-Info.plist` = project `tesseracthub-480811`).
- **Metro on port 8082** (NOT 8081 — that's taken by the user's unrelated `postiz-pg-admin` container; `postiz-redis` holds 6379). Start: `cd apps/mobile-customer && npx expo start --port 8082`. Rebuild: `npx expo run:ios --device <UDID> --port 8082` (kill ALL prior `xcodebuild`/`expo run:ios`/`XCBBuildService` first or you get a build.db lock; `buildReactNativeFromSource:true` so first build is long).
- **Login/driving = `idb`** (installed: `idb_companion` via `brew tap facebook/fb` + `brew trust facebook/fb`; python client at `/Users/Mahesh.Sangawar/Library/Python/3.9/bin/idb`; put `/opt/homebrew/bin` on PATH). Use `idb ui describe-all` for element coords (points), `idb ui tap X Y`, `idb ui text "..."`, `idb ui key 42` (backspace), `idb ui swipe x1 y1 x2 y2`. Screenshot via `xcrun simctl io <UDID> screenshot f.png`.
- **Test login:** `demo@test.com` / `Test@123` (real prod customer "Mahesh Sangawar"). ⚠️ idb text entry is flaky with `@` — type email/password in TWO parts and VERIFY with a screenshot before Sign in; clear fields with `idb ui key 42` if a prior attempt left junk. A relaunch (terminate+launch) drops back to login; a Metro hot-reload keeps the session.
- `cliclick` installed but useless (no macOS Accessibility permission — `-1719`). `docker-compose.override.yml` (gitignored, in `.git/info/exclude`) remaps local API→8090 + drops redis host port — only needed if running the LOCAL api, NOT for the prod-connected sim.
- **Verified on sim:** #371 fully (chef page has no tiffin/group CTAs → straight to menu; Profile MORE section entirely gone; Home row has no Catering/Social Feed chips). #369/#370 need their backends DEPLOYED to fully work (prod lacks the new fields → frontend defaults).

## Constraints
Feature-branch + PR; owner merges. `gh auth switch --user mahesh-sangawar` before tesserix gh ops. Repo is PUBLIC (never commit secrets; auto-reverts to private overnight → re-flip to unblock CI). Deploy = Kargo + GAR-mirror + warehouse refresh + ksvc poll. Mobile: `@babel/core ^7` + `tailwindcss ^3`; vendor tsc clean except pre-existing `app/reviews.tsx`. Single-line commits, no signatures. Never show raw error codes to users.

## Memories to read first
[[project_weekly_menu_scheduling]], [[project_fulfillment_model_settled]], [[reference_eas_monorepo_build]], [[reference_apple_eas_identifiers]], [[project_borzo_3pl_pivot]], [[project_shadowfax_integration_state]], [[reference_kargo_deploy_gar_mirror]], [[reference_deploy_pipeline_and_repo_public]], [[feedback_feature_branch_pr_workflow]], [[reference_customer_sim_live_qa]], [[feedback_local_sim_builds_only]], [[project_ios_native_build_from_source]].

## Suggested next steps
1. Get the owner to review/merge #369, #370, #371 (in that order is fine; independent). After merge + deploy, re-verify #369/#370 behavior on the sim.
2. Owner-side P0s (#25/#14/#19/#20) — produce a step-by-step runbook if not done.
3. Borzo: when the owner pastes the test `X-DV-Auth-Token`, live-validate (calculate→create→status→cancel), store in Secret Manager, flip DB provider row, PR, deploy.
4. Optional: #8 E2E lifecycle test, #11 a11y/bundle audit.
5. Consider building the VENDOR app on the sim to eyeball the #370 "Available days" chips (UI-only vs prod until #370 backend deploys).
