# Handoff — Vendor Payout Epic (2026-07-05)

Continuation of the vendor-payout work (epic **#403**, ADR `docs/adr/0001-vendor-payout-settlement-model.md`). Read the memory `project_vendor_payout_model_finalized` for the full decision record. **Everything below is behind escrow flags `ORDER_PAYOUT_AUTO_RELEASE_ENABLED` / `MEAL_PLAN_ESCROW_ENABLED` (both OFF) — no real money moves until they flip.**

## What was built + merged this session (all on `main`)
The complete escrow control plane, end-to-end:
- **#390** — 6% commission (runtime, flat) + Route-split reconciliation: chef transfer now pays NET (=`ComputeOrderEarnings(order).NetPayout`), reconciled with statements via a **frozen `Order.CommissionRate`** column. Formula: `gross = food + food-GST(Tax) + tip` (tax in, delivery out), `net = gross − 6% − 1% TDS`. Owner decision: chef receives food-GST.
- **#391** — chef self-attest gate (chef `→Delivered` only for `chef_delivery`/`pickup`; 3PL blocked).
- **#387** — payout hold state machine (`awaiting_customer_confirmation → release_eligible → released`/`disputed`) + customer confirm endpoints + 24h auto-confirm sweep + NATS hold events.
- **#388** — admin release queue (backend actuator `services/payout_release.go`/`handlers/admin_payout.go`: release/withhold/reverse/bulk, race-safe) + tesserix-home UI (`app/admin/apps/homechef/payout-queue/page.tsx`, "Release Queue" nav).
- **#456 (P0)** — group-order payouts routed through the hold machine + flag-gated (was leaking live money on delivery).
- **#457 (P0)** — refund↔payout cross-guard (refund drives the hold: withhold/reverse; wired into all 5 order-refund paths + `refunded_at` release backstop across order/day/group).
- **#459** — drift reconcile (`payout_settled_at` + `payout-reconcile` cron re-drives `released`/`reversed`-but-unpaid holds).
- **#461 step 1** — tesserix-home admin gateway signer now binds identity into the BFF HMAC (was signing legacy 4-field, `X-User-Role` unsigned).

## Key backend files (apps/api)
- `services/earnings.go` (ComputeOrderEarnings — the one formula), `services/commission.go`, `handlers/payment.go` (`chefNetPayout`, `orderSettlements`).
- `services/payout_hold.go` (hold state machine + confirm), `services/payout_release.go` (#388 actuator + `WithholdOrReverseOrderHoldForRefund` + `releaseBlockedForAgg`), `services/payout_auto_confirm_cron.go`, `services/payout_reconcile_cron.go`, `services/group_order_payout.go`.
- `models/payout_hold.go` (enum), `models/order.go` / `models/meal_plan.go` / `models/group_order.go` (hold fields).

## Remaining work (all P1/P2, flag-gated, none blocking) — good for a fresh session
- **#461 step 2 (deploy-ordered):** after tesserix-home #26 rolls out (+ confirm auth-bff is bound-only — it already signs 8-field), REMOVE the `computeLegacy` fallback in `apps/api/middleware/bff_auth.go` + add a reject-legacy-with-identity test. DO NOT remove before #26 deploys or admin requests 401. Also add `SPManagePayouts` granular staff-permission on the payout routes.
- **#458** — disputed hold is a dead-end: no transition out of `disputed`; wire `AdminResolveIssue`/`AdminRejectIssue` to drive the hold; surface disputed holds in the queue.
- **#460** — hold-column races: chef `UpdateOrderStatus` `tx.Save(&order)` lost-update (clobbers hold cols) → use targeted `Updates`; make the dispute check atomic with the confirm transition (fold into the guarded UPDATE).
- **#462** — medium cluster: GST-split rounding, partial-amount reverse, gross queue amount, `ReleaseDayPayout` gateway-idempotency (`FetchTransfer`), bulk error labeling, webhook replay ts/nonce.
- **W-A (before flags-ON):** `CancelGroupOrder` reverse runs outside the guarded tx — route through `ReverseHold` (TODO in code, flag-gated so safe at launch).
- **Follow-ups:** meal-plan-day per-day transfer may still pay GROSS (verify — the #390 fix was orders; `RefundDay`/meal-plan escrow is a separate seam); tesserix-home group-order row label + host confirm UI; `RefundGroupParticipant`→hold-reverse; `CancelOrderItem`/`handleRefundProcessed` webhook refund→hold; TCS §52 (CA-pending); historical WeeklyStatement rows + already-issued TDS Form-16A on old basis (regulatory regeneration decision).

## Gotchas
- **gh account flips to the Civica EMU user** and blocks PR/comment on tesserix repos — run `gh auth switch --hostname github.com --user mahesh-sangawar` before every gh write. (memory `reference_gh_account_civica_emu_switch`)
- **Two repos:** backend in `Home-Chef-App/apps/api`; HomeChef admin UI in `../tesserix-home` (`app/admin/apps/homechef/*`, signed gateway → Go `/admin/*`). The `/payouts` route there is the #389 statement mark-paid page (already exists); the #388 release queue is at `/payout-queue`.
- Money-path changes get **plan → plan-check → independent verify** (3 gates). The plan-checker caught real money-math bugs in #387, #388, #457, #390 — do not skip it. Mid-run LSP "undefined" diagnostics are stale RED-phase noise; always verify with a real `go build`/`go test`.
- **Vitest not installed locally** in tesserix-home; cross-check HMAC vectors via `node -e` + Go instead.

## Open PRs
- Home-Chef-App **#443** — UPI-first checkout (parked on the owner toggling Razorpay Test-Mode UPI on).

## Before enabling escrow flags (the real gate)
Fix #461 step 2, #458, #460, W-A, and verify the meal-plan-day-gross follow-up; then #218 sandbox sign-off → #25 live switch. #200/#8 E2E validate; #30 RazorpayX automates disbursement later.
