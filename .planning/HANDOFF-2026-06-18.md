# Session Handoff — 2026-06-18

## TL;DR
Long session. Shipped+merged a batch of backend/CI/test fixes, then ran a **customer-app simulator QA session** that uncovered and fixed a cluster of auth/onboarding bugs (now in **open PR #181**). The headline bug — **cross-account data leak after logout** — is fixed and device-verified.

---

## MERGED this session (all closed)
| PR | What | Issue |
|----|------|-------|
| #100 | vendor login auth errors → `resolveAuthErrorMessage` (no raw codes) | error-code UX |
| #102 | DPDP export/delete Go tests (11) | #12 ✅ |
| #105 | removed obsolete stub file → lucide tsc 48→5 | lucide |
| #107/#108 | DPDP delete `.Unscoped()` → idempotent retry | #106 ✅ |
| #109 | Trivy gate → fail on fixable CRITICAL/HIGH | #15 ✅ |
| #110 | order refund/recompute money-math tests | #8 (partial) |
| #111 | stop logging raw email PII (DPDP + staff-invite) | finding |
| #112 | subscription proration tests (clock-injectable) | #13 (partial) |
Also closed #10/#22/#66. Issues assigned to owner.

## ⏳ OPEN — PR #181 (the active thing to finish)
Branch `fix/query-cache-clear-on-auth-change`. 7 fixes from the device QA, all committed+pushed:
1. **Cross-account cache leak** — `_layout.tsx` (all 3 apps) clears React Query cache when user id changes. **Device-verified ✅** (this was the original "signed up as mayu, saw mahesh" bug).
2. **Signup routed to dashboard not onboarding** — customer `register.tsx` routes by `onboardingComplete`. **Verified ✅**
3. **Onboarding not prefilled** — `user-info.tsx` prefills name/phone from auth store. **Verified ✅**
4. **logout didn't reset onboarding flag** — `useAuth.ts` `logout()` clears the *persisted* `onboardingComplete`. **PENDING re-verify** (caused next signup to skip onboarding → blank profile).
5. **Firebase errors → generic message** — `resolveAuthErrorMessage` now matches `err.code` (+ `auth/invalid-credential`). **PENDING re-verify.**

**Next step:** re-verify #4 + #5 on the sim (⌘R to reload, then: from a signed-in Profile → Sign out → sign up fresh email → should go *through onboarding*, name saved). Then PR #181 is ready to merge.

## Device/sim QA setup (how to resume testing)
- **App:** customer, Debug build, running on **iPhone 17 Pro sim** `9F9E4972-E434-47CD-B7DA-8BE294D41B97`. Bundle `com.tesserix.homechef.customer`. .app at `~/Library/Developer/Xcode/DerivedData/HomeChef-awztvnuwjojqmyhafknleyctmewt/Build/Products/Debug-iphonesimulator/HomeChef.app`.
- **Build cmd (Xcode 26 needs RN-from-source for the firebase/`Copy XCFrameworks`/`Libtool` error):**
  `cd apps/mobile-customer && RCT_USE_PREBUILT_RNCORE=0 npx expo run:ios --device <sim-udid>` (matches eas.json). `buildReactNativeFromSource:true` is already in app.json but local `expo run:ios` ignores it — the env var is what works. `[ReactNativeCore] Building from source: true` = success.
- **Metro/sim likely dead after session end** — rebuild/relaunch as above. JS changes Fast-Refresh; if cache acts stale, `npx expo start -c` (cleared cache) then relaunch.
- **Keychain gotcha:** expo-secure-store survives `simctl uninstall` on the sim → a stale session token makes the app skip login → onboarding. For a clean session, **`xcrun simctl erase <udid>`** then reinstall (uninstall is NOT enough).

## Backend test-account cleanup done
- Deleted `mayu.b14` + `mahesh.sangawar` **customer-pool** rows (DB) + their Firebase users (tenant `HomeChef-Customer-rqg8a`) — multiple rounds during QA. **mahesh.sangawar BUSINESS/chef account preserved.**
- `mayu.s@gmail.com` currently exists (created during QA, skipped onboarding → blank profile; symptom of the now-fixed #4). 1 orphan test order left in DB (harmless).
- **Firebase user delete** (no console needed): Identity Toolkit REST + gcloud token:
  `curl -X POST https://identitytoolkit.googleapis.com/v1/projects/tesseracthub-480811/tenants/HomeChef-Customer-rqg8a/accounts:{lookup,delete} -H "Authorization: Bearer $(gcloud auth print-access-token)" -H "x-goog-user-project: tesseracthub-480811" -d '{"email":["..."]}'` / `-d '{"localId":"..."}'`. (Watch the `UID` shell-var collision — use a different loop var.)

## Design follow-ups noted (not fixed)
- `onboardingComplete` is **device-local**, not backend-derived → survives reinstall poorly, multi-device re-onboards. Logout-reset (#4) fixes the immediate bug; deriving from profile-completeness is the robust fix.
- App treats **token-presence = authenticated** without validating the session → a stale/invalid token (e.g. after server-side account delete) lands the user in a broken authed state until a 401 logs them out. Consider validating session on launch.
- Apple **Hide My Email** relay → separate account (can't email-link); safe but unhandled. Future: explicit "link account" flow in Settings, not email-matching.

## Other open (unchanged)
- Prod DB health: earlier "Not enough disk space" on the CNPG cluster + `homechef-postgres-10` CrashLoop (owner set aside). Also noticed `homechef-api-temporal-worker` in CrashLoopBackOff (Sam's Temporal epic #116–127, not ours).
- Device/live-gated test tickets (#8/#11/#13/#61/#62/#63/#64 remainders), owner-gated launch blockers (#14/#19/#20/#25).
- **Payments structuring (parked):** going live on Razorpay needs an India entity + Indian bank acct. Cleanest = wholly-owned Indian **Pvt Ltd** (Director 2 qualifies as resident director — 182+ days). The existing **partnership firm is NOT reusable** (foreign co can't be a partner; person-B exit forces conversion anyway). Stripe can't collect domestic INR. Needs a CA for FEMA/repatriation (#31). Test mode unblocked for all dev.

## Key context
- **Workflow:** feature-branch + PR; owner authorized merging the fixes this session (Sam normally merges). gh on `mahesh-sangawar` (`gh auth switch --user mahesh-sangawar`).
- **No raw error codes to users** — use `resolveAuthErrorMessage` (now `err.code`-aware).
- **Keep GitHub issues updated** (comment on delivery, open follow-ups, assign to owner) — owner directive.
- **Prod DB:** GKE `tesseract-prod-in-gke` (asia-south1, proj tesseracthub-480811), ns `homechef`, pod `homechef-postgres-7`, db `homechef_db`. `kubectl exec -i ... -c postgres -- psql -U postgres -d homechef_db`.
</content>
